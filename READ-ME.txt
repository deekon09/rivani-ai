V45.8.2 — FRESH-FILENAME EXPORT HOTFIX
Upload exactly these TWO files to the ROOT of deekon09/rivani-ai:
  video-studio-v4582.js   (NEW file, do not rename)
  video-studio.html       (REPLACE existing HTML)
Do not upload the README. Do not delete video-studio-v457.js or v458.js.
Audio Repair, other pages, settings, AI models and CSS are untouched.

WHY:
GitHub main already contains recorderBitrate in video-studio-v458.js, but
the screenshot still shows the old missing-function error. This new
module filename cannot resolve to a cached old video-studio-v458.js.
The new file itself declares recorderBitrate(w,h).

After both files appear in GitHub main, allow hosting deployment to finish.
Open https://rivaniai.online/video-studio.html?v=4582 and refresh.
Look for the engine label starting 'V45.8.2 OFFLINE EXPORT READY'.
If it does not appear, the LIVE site is serving an old deployment/cached HTML.
If it appears and export raises a DIFFERENT error, send that exact message.

This resolves this specific missing-helper ReferenceError. Offline video
decoding/encoding at 4K60 and audio sync require actual browser testing.
