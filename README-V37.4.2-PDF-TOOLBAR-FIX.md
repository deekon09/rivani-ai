RIVANI AI V37.4.2 — PDF Result Toolbar Fix

Changed file:
- image-to-text-pdf.css

Fix:
- Privacy Pattern Scan no longer overflows to the right.
- PDF result action buttons now wrap cleanly inside the result card.
- Desktop/tablet/mobile responsive behavior improved.
- No OCR, PDF extraction, language, model, auth or processing logic changed.

After deploy, hard-refresh once (Ctrl+Shift+R) if the old CSS is cached.
