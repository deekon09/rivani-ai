# Internal model notes — not user-facing

Background Voices candidate: SepFormer WHAMR 16 kHz ONNX community export, Apache-2.0; validate browser ONNX fidelity and multilingual behavior before release.

Music Control candidate: UVR MDX-Net 9482; 44.1 kHz, n_fft 4096, hop 1024, dim_f 2048, dim_t 256, NCHW [1,4,2048,256], complex plane order L_re,L_im,R_re,R_im, predicts vocals.

Keep these names out of normal product UI.
