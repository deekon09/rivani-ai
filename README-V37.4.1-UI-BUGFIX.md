# RIVANI AI V37.4.1 — UI Bug Fix

Changed/new files only.

## Fixed
- Prevents duplicate **Contact** in the top navigation after the extensionless URL migration.
- Uses `/contact` for any Contact link that needs to be auto-added.
- Prevents the same duplicate logic in the Company footer column.
- Re-centers the Audio Repair upload orb inside the Upload Audio pane.

## Not changed
- Audio processing/model/DSP logic
- Image Enhancer processing/model
- Background Remover processing/model
- Image Compressor processing
- Image/PDF OCR processing
- Auth/payment/backend behavior

`/#tools` is intentionally unchanged. It is the homepage `#tools` section anchor.
